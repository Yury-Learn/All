#astra #kvm

Вот короткий `README.md` — можно положить в `/root/vm-migrate/att-sdb2/README.md`.

````markdown
# Перенос ВМ `att-sdb2` c 22.137 → 21.66 (WebVirtCloud/Astra KVM)

Короткий чек-лист выполненных действий и команд.

## 0) Контекст
- Источник: `22.137` (`/mnt/data/libvirt/images/att-sb2.qcow2`)
- Приёмник: `21.66` (`/mnt/data/libvirt/images/`)
- Тип: **cold migration** (с остановкой ВМ)
- Требования PT SB: **RAW thick 400 ГБ**, **Secure Boot OFF**, **нет swap**, nested-вирт ON

---

## 1) Подготовка приёмника (21.66)
```bash
# Nested KVM включён?
cat /sys/module/kvm_intel/parameters/nested  # ожидаем: Y

# Выделяем толстый RAW под 400ГБ
fallocate -l 400G /mnt/data/libvirt/images/att-sdb2.raw
chown libvirt-qemu:kvm /mnt/data/libvirt/images/att-sdb2.raw
chmod 660 /mnt/data/libvirt/images/att-sdb2.raw

# Файлы OVMF без Secure Boot (используем *_4M.fd)
ls /usr/share/OVMF/OVMF_CODE_4M.fd /usr/share/OVMF/OVMF_VARS_4M.fd
mkdir -p /var/lib/libvirt/qemu/nvram
````

---

## 2) Подготовка источника (22.137)

```bash
# Снятие снапшотов и остановка ВМ
virsh snapshot-list att-sdb2
for s in $(virsh snapshot-list --name att-sdb2); do virsh snapshot-delete att-sdb2 --snapshotname "$s"; done
virsh shutdown att-sdb2 && while [ "$(virsh domstate att-sdb2)" != "shut off" ]; do sleep 2; done

# Проверка qcow2
qemu-img check /mnt/data/libvirt/images/att-sb2.qcow2
qemu-img info  /mnt/data/libvirt/images/att-sb2.qcow2
```

Копирование диска на 21.66:

```bash
# Предпочтительно rsync (иначе scp)
rsync -aHAX --inplace --sparse --info=progress2 \
  /mnt/data/libvirt/images/att-sb2.qcow2 \
  root@10.38.21.66:/mnt/data/libvirt/images/att-sdb2.qcow2
```

---

## 3) Конвертация диска (21.66)

```bash
# В уже предвыделённый RAW (не создавать заново! -n)
qemu-img convert -p -O raw -n \
  /mnt/data/libvirt/images/att-sdb2.qcow2 \
  /mnt/data/libvirt/images/att-sdb2.raw

qemu-img info /mnt/data/libvirt/images/att-sdb2.raw
```

---

## 4) Определение ВМ на приёмнике (21.66)

Правки XML (через `micro`/`virsh edit`):

- OVMF **без Secure Boot**:
    
    ```xml
    <os firmware='efi'>
      <type arch='x86_64' machine='pc-q35-8.2'>hvm</type>
      <loader readonly='yes' type='pflash'>/usr/share/OVMF/OVMF_CODE_4M.fd</loader>
      <nvram template='/usr/share/OVMF/OVMF_VARS_4M.fd'>/var/lib/libvirt/qemu/nvram/att-sdb2_VARS.fd</nvram>
      <bootmenu enable='yes'/>
    </os>
    ```
    
- Диск RAW (толстый):
    
    ```xml
    <disk type='file' device='disk'>
      <driver name='qemu' type='raw' io='threads' discard='unmap'/>
      <source file='/mnt/data/libvirt/images/att-sdb2.raw'/>
      <target dev='vda' bus='virtio'/>
      <boot order='1'/>
    </disk>
    ```
    
- CPU/RAM (ограничение хоста: **22 vCPU**, RAM ≈ **51 ГиБ**):
    
    ```xml
    <memory unit='KiB'>53477376</memory>
    <currentMemory unit='KiB'>53477376</currentMemory>
    <vcpu placement='static'>22</vcpu>
    <cpu mode='host-passthrough' check='none' migratable='on'>
      <topology sockets='1' cores='11' threads='2'/>
    </cpu>
    ```
    
- Сеть: `default` + MAC `52:54:00:26:49:52`.
    

Определение и старт:

```bash
virsh define /root/vm-migrate/att-sdb2/att-sdb2.dst.xml
virsh start att-sdb2
# при первом старте создастся nvram-файл
ls -lh /var/lib/libvirt/qemu/nvram/att-sdb2_VARS.fd
```

Если попали в UEFI Shell:

- руками запустить `fs0:\EFI\debian\grubx64.efi`
    
- в госте прописать запись:
    
    ```bash
    efibootmgr -c -d /dev/vda -p 1 -L "Debian" -l '\EFI\debian\grubx64.efi'
    efibootmgr -v
    ```
    

---

## 5) Сеть и агент гостя

```bash
# Получение IP (если DHCP): в госте включить DHCP через NM или /etc/network/interfaces
apt update && apt install -y qemu-guest-agent
systemctl enable --now qemu-guest-agent

# На хосте:
virsh domifaddr att-sdb2
```

---

## 6) Расширение ФС до 400 ГБ и отключение swap (в госте)

```bash
# Исправить backup GPT → конец диска
sgdisk -e /dev/vda

# Удалить swap-раздел, растянуть vda2:
swapoff -a
sed -i.bak '/\sswap\s/s/^/# /' /etc/fstab
parted -s /dev/vda rm 3
growpart /dev/vda 2
resize2fs /dev/vda2

# Контроль
lsblk -o NAME,SIZE,FSTYPE,MOUNTPOINT
df -hT /
swapon --show  # пусто
```

---

## 7) Автозапуск и уборка

```bash
# Автозапуск на 21.66
virsh autostart att-sdb2

# Удаление старой ВМ на источнике (после проверки):
# virsh destroy att-sdb2
# virsh undefine att-sdb2 --nvram
# rm /mnt/data/libvirt/images/att-sb2.qcow2
```

---

## 8) Проверки PT SB (чек-лист)

-  **RAW thick 400 ГБ** (`att-sdb2.raw`)
    
-  **Secure Boot OFF** (обычный OVMF *_4M.fd)
    
-  **Swap отсутствует**
    
-  **Nested KVM** включён на хосте (`Y`)
    
-  **Вирт. флаги** в госте (`vmx`/`svm`)
    
-  **vCPU=22**, **RAM≈51 ГиБ**
    
-  Энергосбережение **отключено в BIOS/UEFI** хоста (ручная проверка)
    

---

## 9) WebVirtCloud

- Добавить/обновить compute `21.66` (`qemu+ssh://root@10.38.21.66/system`)
    
- `Rescan/Sync` для подхвата `att-sdb2`
    
- Удалить старую запись на `22.137`, чтобы не было дублей
    

---

## 10) Трюки/диагностика

```bash
# Показ IP с разных источников
virsh domifaddr att-sdb2
virsh domifaddr att-sdb2 --source lease
virsh net-dhcp-leases default

# Быстрый прогон селективного вывода (sed)
virsh dumpxml att-sdb2 | sed -n '1,120p'   # печать 1..120 строк
```
```
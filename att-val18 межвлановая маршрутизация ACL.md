## Как проверить, что межвлановая маршрутизация/ACL на 172.19.1.1 уже есть

Идея: **убрать костыли (SVI) для теста** и убедиться, что трафик между 172.19.1.0/28 (хост) и 172.19.[2–6].0/28 (ВМ) ходит **через 172.19.1.1**.

### 2.1 Мини-тест для одной VLAN (пример: VLAN 320 / ВМ 172.19.2.2)

1. Временно опустим SVI для этой VLAN (чтобы трафик пошёл через 172.19.1.1):
    
    `sudo nmcli con down br1.320`
    
2. С хоста проверим связность **с исходником из 172.19.1.2**:
    
    `ping -I 172.19.1.2 -c3 172.19.2.2 traceroute -s 172.19.1.2 -n 172.19.2.2 2>/dev/null || true`
    
    Ожидаемо, первый hop будет **172.19.1.1**, и пинг пройдёт.
    
3. С ВМ `att-vmal1` проверим в обратную сторону:
    
    `# внутри ВМ: ping -c3 172.19.1.2 traceroute -n 172.19.1.2 2>/dev/null || true`
    
4. Вернём SVI (чтобы не терять доступ во время проекта):
    
    `sudo nmcli con up br1.320`
    

### 2.2 Критерии «маршрутизация/ACL готовы»

- Пинги **и туда, и обратно** проходят **при выключенном SVI** для VLAN 320.
    
- `traceroute` показывает, что первый (или один из первых) hop — **172.19.1.1**.
    
- На хосте при этом:
    
    `ip r | grep -E 'default via 172\.19\.1\.1|172\.19\.1\.0/28'`
    
    (У нас уже есть дефолт через 172.19.1.1 с метрикой 400 — это ок.)
    

### 2.3 Если тест не пройден — что проверить на 172.19.1.1

- На устройстве **172.19.1.1** (L3-коммутатор/роутер) должны быть:
    
    - SVI/интерфейсы для всех VLAN (адреса **172.19.2.1 / 3.1 / 4.1 / 5.1 / 6.1**) **или** маршруты в эти /28 через 172.19.1.2 (если вы хотите роутить на хост).
        
    - **ACL** должны разрешать ICMP/SSH между 172.19.1.0/28 и 172.19.[2–6].0/28.
        
- Если это управляет другая команда, можно дать им «чек-лист»:
    
    - «Разрешите межвлановую маршрутизацию и добавьте ACL allow между 172.19.1.0/28 и 172.19.[2–6].0/28 (ICMP, TCP/22 минимум). SVIs 172.19.2.1/3.1/4.1/5.1/6.1 на соответствующих VLAN. Проверка по пингам и traceroute.»